Good. Keep the current git history as-is.

Next step: implement a minimal extraction MVP only.
Do not implement change detection yet.

Use the existing project and schema without redesigning anything.

Requirements:

1. Create these files:

   * `src/pipeline/extract_fields.py`
   * `src/pipeline/run_extraction.py`

2. Implement field extraction from `raw_snapshots.raw_text` and `raw_snapshots.page_title`.

3. For the MVP, extract only these fields:

   * `email`
   * `phone`
   * `opening_hours`
   * `fee_text`
   * `pdf_links_json`
   * `important_links_json`

4. Extraction rules:

   * `email`: first detected email address or null
   * `phone`: first detected phone-like string or null
   * `opening_hours`: best-effort text snippet if hours-related keywords are found, otherwise null
   * `fee_text`: best-effort text snippet if fee/tariff/price keywords are found, otherwise null
   * `pdf_links_json`: JSON array of detected PDF links from the page text if possible, otherwise empty array
   * `important_links_json`: JSON array, keep it empty for now if link extraction is not available from raw_text alone

5. `run_extraction.py` should:

   * select the latest snapshot per source from `raw_snapshots`
   * skip snapshots that already have a row in `extracted_fields`
   * insert one row into `extracted_fields` per processed snapshot
   * print a short summary at the end:

     * snapshots considered
     * inserted rows
     * skipped rows

6. Keep the logic simple and deterministic:

   * use regex and straightforward text rules
   * no LLMs
   * no external APIs

7. Do not modify:

   * `00_schema.sql`
   * `01_views.sql`
   * dashboard code
   * ingestion logic

8. After implementation:

   * run the extraction once
   * show me the exact contents of:

     * `src/pipeline/extract_fields.py`
     * `src/pipeline/run_extraction.py`
   * show a short database summary:

     * number of rows now in `extracted_fields`
     * 3 sample extracted rows
