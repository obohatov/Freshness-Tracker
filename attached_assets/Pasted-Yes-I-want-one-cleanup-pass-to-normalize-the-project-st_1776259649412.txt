Yes — I want one cleanup pass to normalize the project structure.

We currently have folder confusion between:

* `/home/runner/workspace`
* `/home/runner/workspace/freshness-tracker`

I want a single canonical project root.

## Goal

Make `/home/runner/workspace` the canonical project root for this repository and eliminate the duplicated nested project structure safely.

## Rules

* Do not redesign the project.
* Do not change schema or pipeline logic unless a tiny path fix is required.
* Do not delete anything blindly.
* First verify and compare, then normalize.
* Show exact filesystem state and exact changes.

## Tasks

1. Confirm the git root with:

   * `git rev-parse --show-toplevel`

2. Treat that git root as the canonical project root.

3. Compare the canonical root against the nested duplicate folder:

   * `/home/runner/workspace/freshness-tracker`
     Check whether any files exist there that are newer or different from the canonical root versions.

4. If a file in the nested folder is newer or contains important changes, copy/merge it into the canonical root version.

5. After that, normalize the canonical root so it contains exactly the project files and folders at top level, including:

   * `README.md`
   * `.env.example`
   * `.gitignore`
   * `requirements.txt`
   * `.streamlit/config.toml`
   * `dashboard/`
   * `data/`
   * `scripts/`
   * `sql/`
   * `src/`
   * optional `docs/screenshots/`

6. Update README paths so they describe the canonical root correctly.
   Important:

   * do NOT describe the project as if it lives inside a nested `freshness-tracker/` folder anymore
   * the project tree in README must match the canonical root

7. After normalization, either:

   * remove the nested duplicate folder if it is fully redundant,
     OR
   * rename it to something clearly archival like `freshness-tracker_old/`
     if safe deletion is uncertain

8. Then show me:

   * the exact final project tree from the canonical root
   * the exact final location of `README.md`
   * the exact final location of `.streamlit/config.toml`
   * `git status`
   * `git diff --stat`
   * the first 120 lines of the final `README.md`

9. Do not commit yet.
   I want to review the normalized structure first before committing.

## Important

The purpose of this step is only to fix structure/path confusion and make the repository internally consistent.
Do not add new features in this pass.
